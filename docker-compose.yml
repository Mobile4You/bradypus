version: '2'

services:
  app:
    build: .
    image: bradypus
    command: amber watch
    environment:
      DATABASE_URL: postgres://admin:password@db:5432/bradypus_development
    ports:
      - 3000:3000
    links:
      - db
    volumes:
    - .:/app
    - nodes:/app/node_modules
    - shards:/app/lib

  migrate:
    build: .
    image: bradypus
    command: bash -c 'while ! nc -q 1 db 5432 </dev/null; do sleep 1; done && amber db migrate seed'
    environment:
      DATABASE_URL: postgres://admin:password@db:5432/bradypus_development
    volumes:
      - .:/app
    links:
      - db

  db:
    image: postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: bradypus_development
    ports:
      - 5432:5432
    volumes:
      - db:/var/lib/postgres/data
    ports:
      - "5432:5432"

volumes:
  db:
  nodes:
  shards:
